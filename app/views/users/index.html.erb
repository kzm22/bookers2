<%= render 'books/sidebar', books: @books %>
    <div class = "col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class = "table table-hover table-inverse">
        <thead>
          <th>Image</th>
          <th>name</th>
          <th></th>
        </thead>
        <% @users.each do |user| %>
        <tbody>
          <tr>
            <td><%= image_tag user.get_profile_image(100,100) %></td>
            <td><%= user.name %></td>
            <td><%= link_to "Show",url: user_path(user),method: :get %></td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>